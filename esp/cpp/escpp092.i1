/*****************************************************************************
**
**   cd9203.i1 - Campos item x item-uni-estab
**
******************************************************************************/

     when "cap-est-fabr":u then
          return string ({1}.cap-est-fabr).
     when "cd-planejado":u then
          return string ({1}.cd-planejado).
     when "char-1":u then
          return string ({1}.char-1).
     when "char-2":u then
          return string ({1}.char-2).
     when "check-sum":u then
          return string ({1}.check-sum).
     when "ciclo-contag":u then
          return string ({1}.ciclo-contag).
     when "classe-repro":u then
          return string ({1}.classe-repro).
     when "classif-abc":u then
          return string ({1}.classif-abc).
     when "cod-comprado":u then
          return string ({1}.cod-comprado).
     when "cod-estabel":u then
          return string ({1}.cod-estabel).
     when "cod-localiz":u then
          return string ({1}.cod-localiz).
     when "cod-obsoleto":u then
          return string ({1}.cod-obsoleto).
     when "consumo-aad":u then
          return string ({1}.consumo-aad).
     when "consumo-prev":u then
          return string ({1}.consumo-prev).
     when "contr-plan":u then
          return string ({1}.contr-plan).
     when "contr-qualid":u then
          return string ({1}.contr-qualid).
     when "conv-tempo-seg":u then
          return string ({1}.conv-tempo-seg).
     when "criticidade":u then
          return string ({1}.criticidade).
     when "curva-abc":u then
          return string ({1}.curva-abc).
     when "data-1":u then
          return string ({1}.data-1).
     when "data-2":u then
          return string ({1}.data-2).
     when "data-base":u then
          return string ({1}.data-base).
     when "data-ult-con":u then
          return string ({1}.data-ult-con).
     when "data-ult-ent":u then
          return string ({1}.data-ult-ent).
     when "data-ult-rep":u then
          return string ({1}.data-ult-rep).
     when "data-ult-sai":u then
          return string ({1}.data-ult-sai).
     when "dec-1":u then
          return string ({1}.dec-1).
     when "dec-2":u then
          return string ({1}.dec-2).
     when "demanda":u then
          return string ({1}.demanda).
     when "deposito-pad":u then
          return string ({1}.deposito-pad).
     when "div-ordem":u then
          return string ({1}.div-ordem).
     when "emissao-ord":u then
          return string ({1}.emissao-ord).
     when "fator-refugo":u then
          return string ({1}.fator-refugo).
     when "horiz-fixo":u then
          return string ({1}.horiz-fixo).
     when "ind-calc-meta":u then
          return string ({1}.ind-calc-meta).
     when "ind-prev-demanda":u then
          return string ({1}.ind-prev-demanda).
     when "int-1":u then
          return string ({1}.int-1).
     when "int-2":u then
          return string ({1}.int-2).
     when "it-codigo":u then
          return string ({1}.it-codigo).
     when "loc-unica":u then
          return string ({1}.loc-unica).
     when "log-1":u then
          return string ({1}.log-1).
     when "log-2":u then
          return string ({1}.log-2).
     when "lote-economi":u then
          return string ({1}.lote-economi).
     when "lote-minimo":u then
          return string ({1}.lote-minimo).
     when "lote-multipl":u then
          return string ({1}.lote-multipl).
     when "nat-despesa":u then
          return string ({1}.nat-despesa).
     when "nr-linha":u then
          return string ({1}.nr-linha).
     when "periodo-fixo":u then
          return string ({1}.periodo-fixo).
     when "perm-saldo-neg":u then
          return string ({1}.perm-saldo-neg).
     when "politica":u then
          return string ({1}.politica).
     when "preco-base":u then
          return string ({1}.preco-base).
     when "preco-fiscal":u then
          return string ({1}.preco-fiscal).
     when "preco-repos":u then
          return string ({1}.preco-repos).
     when "preco-ul-ent":u then
          return string ({1}.preco-ul-ent).
     when "prioridade":u then
          return string ({1}.prioridade).
     when "qt-max-ordem":u then
          return string ({1}.qt-max-ordem).
     when "qtd-batch-padrao":u then
          return string ({1}.qtd-batch-padrao).
     when "qtd-refer-custo-dis":u then
          return string ({1}.qtd-refer-custo-dis).
     when "quant-perda":u then
          return string ({1}.quant-perda).
     when "quant-segur":u then
          return string ({1}.quant-segur).
     when "rep-prod":u then
          return string ({1}.rep-prod).
     when "reporte-ggf":u then
          return string ({1}.reporte-ggf).
     when "reporte-mob":u then
          return string ({1}.reporte-mob).
     when "res-cq-comp":u then
          return string ({1}.res-cq-comp).
     when "res-cq-fabri":u then
          return string ({1}.res-cq-fabri).
     when "res-for-comp":u then
          return string ({1}.res-for-comp).
     when "res-int-comp":u then
          return string ({1}.res-int-comp).
     when "ressup-fabri":u then
          return string ({1}.ressup-fabri).
     when "sit-aloc":u then
          return string ({1}.sit-aloc).
     when "tempo-segur":u then
          return string ({1}.tempo-segur).
     when "tipo-est-seg":u then
          return string ({1}.tipo-est-seg).
     when "tipo-lote-ec":u then
          return string ({1}.tipo-lote-ec).
     when "tipo-requis":u then
          return string ({1}.tipo-requis).
     when "tipo-sched":u then
          return string ({1}.tipo-sched).
     when "tp-aloc-lote":u then
          return string ({1}.tp-aloc-lote).
     when "val-fator-custo-dis":u then
          return string ({1}.val-fator-custo-dis).
     when "var-mob-maior":u then
          return string ({1}.var-mob-maior).
     when "var-mob-menor":u then
          return string ({1}.var-mob-menor).
     when "var-rep":u then
          return string ({1}.var-rep).
     when "var-transf":u then
          return string ({1}.var-transf).
     when "variac-acum":u then
          return string ({1}.variac-acum).
     when "vl-var-max":u then
          return string ({1}.vl-var-max).
     when "vl-var-min":u then
          return string ({1}.vl-var-min).

